name: Identity
path: /identity

routes:
  - upstream: /users/{id}
    method: GET
    use: downstream
    downstream: identity-service/users/{id}
    auth: true
    claims:
      role: admin

  - upstream: /me
    method: GET
    use: downstream
    downstream: identity-service/me
    auth: true

  - upstream: /sign-up
    method: POST
    use: downstream
    downstream: identity-service/sign-up
    auth: false

  - upstream: /sign-in
    method: POST
    use: downstream
    downstream: identity-service/sign-in
    auth: false
    response_headers:
      content-type: application/json

services:
  identity-service:
    local_url: localhost:5004
    url: load_balancer/identity-service