{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://example.com/root.json",
  "type": "object",
  "title": "Complete customer registration schema",
  "required": [
    "customerId",
    "fullName",
    "address"
  ],
  "properties": {
    "customerId": {
      "$id": "#/properties/customerId",
      "type": "string",
      "title": "The CustomerId Schema",
      "default": "",
      "examples": [
        ""
      ],
      "pattern": "^(.+)$"
    },
    "fullName": {
      "$id": "#/properties/fullName",
      "type": "string",
      "title": "The FullName Schema",
      "default": "",
      "examples": [
        ""
      ],
      "pattern": "^(.+)$"
    },
    "address": {
      "$id": "#/properties/address",
      "type": "string",
      "title": "The Address Schema",
      "default": "",
      "examples": [
        ""
      ],
      "pattern": "^(.+)$"
    }
  }
}